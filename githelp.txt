git init

git status ==> always start on branch master

touch readme.md
git status

git add _filename_
or git Add (adds all)

git reset _filename

git commit			opens text editor for commit message
git checkout -b dev 		switch to new branch 'dev'

git merge _branch_			merges _branch_ into current branch

git stash					???

commit --amend				combines commit into previous commit


git push origin granoia

Workflow:
	New Issue
	Create new branch
	Make change
	push to GitHub
	make Pull Request
	

or:
	Clone
	each create branch
	make changes
	merge into master branch